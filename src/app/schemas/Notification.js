import mangoose from 'mongoose';

const NotificationSchema = new mangoose.Schema(
  {
    content: {
      type: String,
      required: true,
    },
    user: {
      type: Number,
      required: true,
    },
    read: {
      type: Boolean,
      required: true,
      default: false,
    },
  },
  {
    timestamps: true,
  }
);

export default mangoose.model('Notification', NotificationSchema);
